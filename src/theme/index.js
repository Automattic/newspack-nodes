/**
 * Theme entry — emits build/theme/index.css, the canonical Newspack token
 * stylesheet shipped as the `newspack-nodes-theme` style handle. No JS; the
 * single SCSS import is the payload (esbuild extracts it to index.css).
 */
import './newspack-theme.scss';
